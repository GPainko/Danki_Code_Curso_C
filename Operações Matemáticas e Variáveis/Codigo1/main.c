#include <stdio.h>

int main(void) {
  // a recebe o valor 10
  int a = 10;
  // b recebe o valor 20
  int b = 20;
  // resuldado recebe o valor da soma de a + b
  int resultado = a + b;
  // imprime na tela o resultado
  printf("o resultado é igaul a : %d", resultado);
  return 0;
}